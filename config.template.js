/* ========================================
   App Configuration Template
   Copy this file as 'config.js' and fill in your own API keys.
   config.js is gitignored so your secrets stay private.
   ======================================== */

const APP_CONFIG = {
    // Gemini Vision AI — get yours at https://aistudio.google.com/
    GEMINI_API_KEY: 'YOUR_GEMINI_API_KEY_HERE',

    // Firebase project config — get from Firebase Console > Project Settings > General
    firebaseConfig: {
        apiKey: 'YOUR_FIREBASE_API_KEY',
        authDomain: 'YOUR_PROJECT.firebaseapp.com',
        projectId: 'YOUR_PROJECT_ID',
        storageBucket: 'YOUR_PROJECT.firebasestorage.app',
        messagingSenderId: 'YOUR_SENDER_ID',
        appId: 'YOUR_APP_ID'
    }
};
